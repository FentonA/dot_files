# Sway — default session ("Sway", "Sway 1.11", "Sway (NVIDIA)")
#
# This is the everything-at-once desk you had before the profiles split: all the
# shared config, plus every app launched onto whatever workspace it lands on.
# Behaviour is unchanged from before — the bindings, colors and rules simply
# live in `common` now so the other session profiles can share them.
#
# For the one-app-per-workspace desk, see profiles/careerplug.config and pick
# "Sway — CareerPlug" at the login screen.

include common

### Startup apps
exec ghostty
exec slack
exec flatpak run com.discordapp.Discord
exec thunderbird
exec ticktick
exec kanshi
