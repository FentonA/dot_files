-- Copy to ~/.config/nvim/dbs.lua (gitignored) and fill in real connections.
-- Returns a table consumed by vim-dadbod-ui as `vim.g.dbs`.
--
-- Keys become the connection names shown in DBUI.
--
-- return {
--   -- Postgres
--   app_dev = "postgresql://user:password@localhost:5432/app_development",
--   app_staging = "postgresql://user:password@staging.example.com:5432/app",
--
--   -- MySQL
--   legacy = "mysql://user:password@localhost:3306/legacy",
-- }
--
-- To keep passwords out of this file too, read them from the environment
-- (set in ~/.config/fish/conf.d/ or a secrets manager):
--
-- return {
--   app_dev = ("postgresql://app:%s@localhost:5432/app_development"):format(vim.env.APP_DB_PASSWORD or ""),
--   legacy = vim.env.LEGACY_DATABASE_URL,
-- }

return {}
